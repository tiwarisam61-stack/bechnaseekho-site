"import React from \"react\";

const H2 = ({ children }) => (
  <h2 className=\"font-serif-editorial text-2xl font-semibold text-zinc-900 mb-3 mt-1 relative\">
    {children}
    <span className=\"block w-10 h-[2px] bg-zinc-900 mt-2\" />
  </h2>
);

export default function Editorial({ data }) {
  const { personal, experience, education, skills, projects, certifications, languages } = data;
  return (
    <div className=\"a4-page p-12 grid grid-cols-3 gap-8 text-[12.5px] text-zinc-800 leading-relaxed font-body\">
      <aside className=\"col-span-1 space-y-6 border-r border-zinc-200 pr-6\">
        <div>
          <h1 className=\"font-serif-editorial text-3xl font-bold text-zinc-900 leading-tight\">
            {personal.fullName || \"Your Name\"}
          </h1>
          <p className=\"text-zinc-600 italic mt-1\">{personal.title}</p>
        </div>
        <div className=\"space-y-1 text-[12px] text-zinc-700\">
          {personal.email && <div>{personal.email}</div>}
          {personal.phone && <div>{personal.phone}</div>}
          {personal.location && <div>{personal.location}</div>}
          {personal.website && <div>{personal.website}</div>}
          {personal.linkedin && <div>{personal.linkedin}</div>}
        </div>

        {skills?.length > 0 && (
          <div>
            <H2>Skills</H2>
            <ul className=\"space-y-1 text-zinc-700\">
              {skills.map((s, i) => <li key={i}>— {s}</li>)}
            </ul>
          </div>
        )}

        {languages?.length > 0 && (
          <div>
            <H2>Languages</H2>
            <ul className=\"space-y-1 text-zinc-700\">
              {languages.map((l, i) => <li key={i}>{l.name} <span className=\"text-zinc-500\">— {l.level}</span></li>)}
            </ul>
          </div>
        )}

        {certifications?.length > 0 && (
          <div>
            <H2>Awards</H2>
            <ul className=\"space-y-2 text-zinc-700\">
              {certifications.map((c, i) => (
                <li key={i}>
                  <div className=\"font-semibold text-zinc-900\">{c.name}</div>
                  <div className=\"text-[11.5px] text-zinc-500\">{c.issuer} · {c.date}</div>
                </li>
              ))}
            </ul>
          </div>
        )}
      </aside>

      <main className=\"col-span-2 space-y-6\">
        {personal.summary && (
          <div>
            <H2>Profile</H2>
            <p className=\"text-zinc-700 first-letter:text-4xl first-letter:font-serif-editorial first-letter:font-bold first-letter:mr-1 first-letter:float-left first-letter:leading-none\">
              {personal.summary}
            </p>
          </div>
        )}

        {experience?.length > 0 && (
          <div>
            <H2>Experience</H2>
            <div className=\"space-y-4\">
              {experience.map((e, i) => (
                <div key={i}>
                  <div className=\"flex justify-between items-baseline\">
                    <div className=\"font-semibold text-zinc-900\">{e.role} <span className=\"text-zinc-500 font-normal\">at {e.company}</span></div>
                    <div className=\"text-[11.5px] text-zinc-500\">{e.startDate} – {e.endDate}</div>
                  </div>
                  {e.location && <div className=\"text-[11.5px] text-zinc-500 italic\">{e.location}</div>}
                  {e.bullets?.length > 0 && (
                    <ul className=\"mt-2 space-y-1 text-zinc-700\">
                      {e.bullets.map((b, j) => <li key={j}>— {b}</li>)}
                    </ul>
                  )}
                </div>
              ))}
            </div>
          </div>
        )}

        {projects?.length > 0 && (
          <div>
            <H2>Selected Work</H2>
            <div className=\"space-y-2\">
              {projects.map((p, i) => (
                <div key={i}>
                  <div className=\"font-semibold text-zinc-900\">{p.name}</div>
                  <div className=\"text-zinc-700\">{p.description}</div>
                </div>
              ))}
            </div>
          </div>
        )}

        {education?.length > 0 && (
          <div>
            <H2>Education</H2>
            {education.map((ed, i) => (
              <div key={i} className=\"flex justify-between items-baseline\">
                <div>
                  <div className=\"font-semibold text-zinc-900\">{ed.school}</div>
                  <div className=\"text-zinc-700\">{ed.degree}{ed.field ? `, ${ed.field}` : \"\"}</div>
                </div>
                <div className=\"text-[11.5px] text-zinc-500\">{ed.startDate} – {ed.endDate}</div>
              </div>
            ))}
          </div>
        )}
      </main>
    </div>
  );
}
"