"import React from \"react\";

export default function BoldCreative({ data }) {
  const { personal, experience, education, skills, projects, certifications, languages } = data;
  return (
    <div className=\"a4-page grid grid-cols-[35%_65%] font-body text-[12.5px] leading-relaxed\">
      <aside className=\"bg-zinc-950 text-zinc-100 p-8 space-y-6\">
        <div>
          <h1 className=\"font-heading text-3xl font-black leading-tight text-white\">
            {personal.fullName || \"Your Name\"}
          </h1>
          <div className=\"w-10 h-1 bg-white my-3\" />
          <p className=\"text-zinc-300 uppercase tracking-widest text-[11px] font-bold\">{personal.title}</p>
        </div>

        <div>
          <h2 className=\"overline text-white mb-2\">Contact</h2>
          <div className=\"space-y-1 text-zinc-300 text-[12px]\">
            {personal.email && <div>{personal.email}</div>}
            {personal.phone && <div>{personal.phone}</div>}
            {personal.location && <div>{personal.location}</div>}
            {personal.website && <div>{personal.website}</div>}
            {personal.linkedin && <div>{personal.linkedin}</div>}
          </div>
        </div>

        {skills?.length > 0 && (
          <div>
            <h2 className=\"overline text-white mb-2\">Skills</h2>
            <div className=\"flex flex-wrap gap-1.5\">
              {skills.map((s, i) => (
                <span key={i} className=\"bg-white/10 text-zinc-100 px-2 py-1 text-[11px] border border-white/20\">{s}</span>
              ))}
            </div>
          </div>
        )}

        {languages?.length > 0 && (
          <div>
            <h2 className=\"overline text-white mb-2\">Languages</h2>
            <div className=\"space-y-1 text-zinc-300 text-[12px]\">
              {languages.map((l, i) => <div key={i}>{l.name} — <span className=\"text-zinc-500\">{l.level}</span></div>)}
            </div>
          </div>
        )}

        {certifications?.length > 0 && (
          <div>
            <h2 className=\"overline text-white mb-2\">Certifications</h2>
            {certifications.map((c, i) => (
              <div key={i} className=\"text-[12px] text-zinc-300 mb-1\">
                <div className=\"text-white font-semibold\">{c.name}</div>
                <div className=\"text-zinc-500\">{c.issuer} · {c.date}</div>
              </div>
            ))}
          </div>
        )}
      </aside>

      <main className=\"p-8 text-zinc-800 space-y-6\">
        {personal.summary && (
          <section>
            <h2 className=\"font-heading text-xl font-black text-zinc-900 mb-2 uppercase tracking-tight\">Profile</h2>
            <p className=\"text-zinc-700\">{personal.summary}</p>
          </section>
        )}

        {experience?.length > 0 && (
          <section>
            <h2 className=\"font-heading text-xl font-black text-zinc-900 mb-3 uppercase tracking-tight\">Experience</h2>
            <div className=\"space-y-4\">
              {experience.map((e, i) => (
                <div key={i} className=\"border-l-4 border-zinc-900 pl-4\">
                  <div className=\"flex justify-between items-baseline\">
                    <div className=\"font-bold text-zinc-900\">{e.role}</div>
                    <div className=\"text-[11.5px] text-zinc-500\">{e.startDate} – {e.endDate}</div>
                  </div>
                  <div className=\"text-zinc-700 mb-1\">{e.company}{e.location ? ` · ${e.location}` : \"\"}</div>
                  {e.bullets?.length > 0 && (
                    <ul className=\"list-disc ml-5 space-y-1 text-zinc-700\">
                      {e.bullets.map((b, j) => <li key={j}>{b}</li>)}
                    </ul>
                  )}
                </div>
              ))}
            </div>
          </section>
        )}

        {projects?.length > 0 && (
          <section>
            <h2 className=\"font-heading text-xl font-black text-zinc-900 mb-3 uppercase tracking-tight\">Projects</h2>
            {projects.map((p, i) => (
              <div key={i} className=\"mb-2\">
                <div className=\"font-bold text-zinc-900\">{p.name}</div>
                <div className=\"text-zinc-700\">{p.description}</div>
              </div>
            ))}
          </section>
        )}

        {education?.length > 0 && (
          <section>
            <h2 className=\"font-heading text-xl font-black text-zinc-900 mb-3 uppercase tracking-tight\">Education</h2>
            {education.map((ed, i) => (
              <div key={i} className=\"flex justify-between mb-2\">
                <div>
                  <div className=\"font-bold text-zinc-900\">{ed.school}</div>
                  <div className=\"text-zinc-700\">{ed.degree}{ed.field ? `, ${ed.field}` : \"\"}</div>
                </div>
                <div className=\"text-[11.5px] text-zinc-500\">{ed.startDate} – {ed.endDate}</div>
              </div>
            ))}
          </section>
        )}
      </main>
    </div>
  );
}
"